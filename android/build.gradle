// Root build script. Plugin versions are pinned here and applied (apply false)
// so the app module can opt in. AGP 8.5.2 + Kotlin 1.9.24 (known-good pair).

plugins {
    id 'com.android.application' version '8.5.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
}
